# DailyChat 작업 규칙

## 새 작업 시작 전 필수 확인

1. `git status --short`, `git diff --name-only`, 최근 커밋을 확인한다.
2. 현재 작업 시작 전에 존재한 수정·추가 파일은 사용자 작업으로 간주한다.
3. 사용자 변경이 다음 작업과 관련되면 diff, 설정, 테스트 또는 외부 상태를 먼저 검증한다.
4. 검증되지 않은 사용자 변경 위에 새 구현을 쌓지 않는다.
5. 사용자 변경을 되돌리거나 덮어쓰거나 임의로 커밋하지 않는다.
6. 새 작업과 충돌하거나 의도가 불명확하면 구현을 멈추고 사용자에게 확인한다.
7. 관련 없는 편집기 상태 파일은 그대로 보존하고 커밋에서 제외한다.

## 작업 완료 기준

- 변경 범위에 맞는 자동 테스트와 실제 연결 검증을 수행한다.
- 비밀값과 사용자 원문이 코드, Git diff, 로그에 포함되지 않았는지 확인한다.
- `MindCompanion-Vault/15 현재 진행 현황.md`와 `06 MVP 백로그.md`를 갱신한다.
- 중요한 구현은 `MindCompanion-Vault/dev-log/`에 근거와 다음 작업을 기록한다.
- 사용자 작업 파일과 에이전트 작업 파일을 구분해 보고한다.
