---
tags: [dev-log, mindcompanion, idempotency, versioning, supabase]
date: 2026-09-25
status: completed
---

# D07-D08 야간 재실행과 일기 버전

## 작업 전 확인

- 기준 커밋: `f62cafa feat: persist nightly results atomically`
- 기존 사용자 변경 `MindCompanion-Vault/.obsidian/workspace.json`은 보존·제외
- Supabase changelog의 Data API 명시 grant 변경과 RPC 함수 권한 지침 재확인
- PostgREST 14의 custom `40001` 재시도 이슈를 확인하고 표준 exception만 사용

## 구현

- Supabase CLI로 migration 골격 생성 후 `0009_nightly_replay_versioning.sql`로 배치
- `prepare_nightly_job`으로 동일 job identity 생성·재사용과 상태별 action 반환
- 동일 identity의 재현 metadata 불일치 거부
- `persist_nightly_extraction_versioned` wrapper로 날짜별 version 원자 할당
- 날짜별 advisory transaction lock 뒤 기존 원자 저장 RPC 호출
- Worker store에 `prepareRun()`과 version-aware save 적용

## 운영 검증

- 첫 입력: prepare created, save saved, diary version 1
- 동일 입력: prepare noop
- 변경 hash: prepare created, save saved, diary version 2
- 성공 job 저장 재호출: noop
- 실제 Data API prepare/save RPC route 확인
- 함수 권한: invoker, 빈 search path, service-role only
- 합성 데이터 전부 rollback, 잔존 0건
- Advisor 새 ERROR/WARN 없음

## 자동 검증

- job identity conflict, 성공 no-op, metadata mismatch 규칙 검사
- advisory lock과 `max(version)+1`, wrapper 호출 검사
- prepare request/response 및 no-op diary 식별자 검증
- `npm run check`: lint, typecheck, 자동 테스트 130개 통과

## 다음 작업

- D09 Telegram notification outbox 등록·claim·발송·재시도
- D10 concern/urgent 자동 발송 보류와 안전 안내 분기
