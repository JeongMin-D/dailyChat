---
tags: [dev-log, nightly, github-actions, supabase, telegram, m2]
date: 2026-09-26
status: verified
---

# M2 첫 live 실행

## 중단 지점 복구

- 대상 날짜: `2026-09-24`
- 입력: 실제 user message 5건
- 실행 전: nightly job 0건, diary 0건, outbox 0건
- 사용자 소유 `.obsidian/workspace.json` 변경은 보존하고 작업에서 제외했다.

첫 live run `36143759657`은 Groq strict Structured Output HTTP 400으로 실패했다. job은 running으로 claim됐지만 diary와 outbox는 생성되지 않았다. 2분 뒤 재실행은 5분 stale 기준 전이라 `not_claimed`로 종료되어 중복 처리를 막았다.

## 복구 구현

- Groq HTTP 400 invalid request/failed generation을 같은 요청으로 최대 3회 제한 재시도한다.
- provider 오류 본문과 failed generation 원문은 로그에 남기지 않는다.
- type, code, failed-generation 존재 여부만 안전한 metadata로 기록한다.
- 자동 회귀 테스트를 추가해 두 번 실패 후 세 번째 성공과 민감정보 비로그를 확인했다.

## 실제 결과

GitHub Actions run `36191065811`이 stale job을 재개해 완료했다.

- job: `succeeded`, attempt 2, error 없음
- diary: version 1
- diary block: 1개
- outbox: `sent`, attempt 1, error 없음
- Telegram: provider message ID와 sent timestamp 저장
- 로그: `nightly_run_finished`, action `completed`
- 전체 자동 테스트: 168개 통과

## 남은 검증

첫 04:05 KST 예약 실행 이력은 아직 없다. 동일 날짜 중복 live 실행은 실제 Telegram 동작 가능성이 있어 추가 승인 후 diary/outbox/전송이 증가하지 않는지 확인한다.
